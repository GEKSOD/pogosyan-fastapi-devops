apiVersion: v1
kind: Service
metadata:
  name: sharay-fastapi
  namespace: sharay
spec:
  type: ClusterIP
  selector:
    app: sharay-fastapi
  ports:
    - name: http
      port: 80
      targetPort: http
